name: Build and Publish Installer

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: installer-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          update: true
          install: >-
            git
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-SDL2
            mingw-w64-x86_64-go

      - name: Install dependencies
        shell: msys2 {0}
        run: |
          export GOROOT=/mingw64/lib/go
          export GOPATH=/mingw64
          go mod download
          go install github.com/tc-hib/go-winres@latest

      - name: Determine version
        id: version
        shell: pwsh
        run: |
          if ("${{ github.ref_type }}" -eq "tag") {
            "tag=${{ github.ref_name }}" >> $env:GITHUB_OUTPUT
          } else {
            "tag=latest" >> $env:GITHUB_OUTPUT
          }

      - name: Build GUI and CLI
        shell: msys2 {0}
        run: |
          export GOROOT=/mingw64/lib/go
          export GOPATH=/mingw64
          go-winres make --product-version "git-tag"
          CGO_ENABLED=1 GOOS=windows GOARCH=amd64 go build -v -tags static -ldflags "-s -w -H=windowsgui -extldflags=-static -X 'vencordinstaller/buildinfo.InstallerGitHash=$(git rev-parse --short HEAD)' -X 'vencordinstaller/buildinfo.InstallerTag=${{ steps.version.outputs.tag }}'" -o VencordArabicInstaller.exe

      - name: Create checksums
        shell: pwsh
        run: |
          $gui = (Get-FileHash VencordArabicInstaller.exe -Algorithm SHA256).Hash.ToLowerInvariant()
          "$gui  VencordArabicInstaller.exe" | Set-Content SHA256SUMS.txt -Encoding ascii

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: VencordArabicInstaller-Windows
          path: |
            VencordArabicInstaller.exe
            SHA256SUMS.txt

      - name: Publish release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "${{ steps.version.outputs.tag }}"
          if ($tag -eq "latest") {
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git tag -f latest $env:GITHUB_SHA
            git push origin refs/tags/latest --force
          }

          $notes = "Vencord Arabic Installer for Windows.`n`nThis is an unofficial fork based on the GPL-licensed Vencord Installer.`nIt downloads builds from ShadowUR0/Vencord and is not affiliated with Discord or the official Vencord team.`n`nSource commit: $env:GITHUB_SHA"
          $notes | Set-Content release-notes.md -Encoding utf8

          gh release view $tag *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release edit $tag --title "Vencord Arabic Installer - $tag" --notes-file release-notes.md
            gh release upload $tag VencordArabicInstaller.exe SHA256SUMS.txt --clobber
          } else {
            gh release create $tag VencordArabicInstaller.exe SHA256SUMS.txt --target $env:GITHUB_SHA --title "Vencord Arabic Installer - $tag" --notes-file release-notes.md
          }
